3월 16일 시행 - PART1
1.   표준 문자열 출력 함수로 자동으로 줄바꿈이 처리되는 함수는 무엇인가?
 1) puts

2) gets

3) readline

4) getchar()

정답 : 1 번


2.   매크로 상수 정의에 사용되는 키워드는 무엇인가?

1) macro
2) typedef
3) include
4) define

정답 : 4 번

3.   사용자 정의 헤더 파일을 선언하는 문장으로 가장 적절한 것은?

1) 5#include <head.h>
2) #define <head.h>
3) #include “head.h”
4) #define “head.h”

정답 : 3 번

4.   계속해서 프로그램을 수행하고자 하는 경우 필요한 구문은?
1) 조건문
2) 반복문
3) 전처리문
4) 함수

정답 : 2 번

5.   매크로 상수에 대한 설명으로 옳은 것은?

1) 매크로 상수로 실수를 사용하는 것이 가능하다.
2) 매크로 상수는 대문자로 기술해야 한다.
3) 매크로 상수에 문자열은 사용할 수 없다.
4) 매크로 상수로 예약어도 사용할 수 있다.

정답 : 1 번


6.   다음 출력결과는 무엇인가?

#define TAX(n)  n*300
int main()
{
     printf(“%d”, TAX(2+3);
     return 0;
}

1) 5
2) 902
3) 1500
4) 25
정답 : 2 번

7.   다음 출력결과는 무엇인가?

#define TAX(n)  n*300
int main()
{
     printf(“%d”, TAX(5);
     return 0;
}

1) 5
2) 300
3) 1500
4) 25
정답 : 3 번

8.   문자형 2차원 배열 3행 10열에 대한 포인터를 보관할 포인터 변수를 선언하시오.
 1) char *(cp)[n]
2) char (*cp)[10];
3) scanf("%s",&n)
4) printf("%s",n)

정답 : 2 번


9.   다음과 같이 선언과 초기화 한경우 30 을 참조할 수 있는 것은?

int a[2] = {10,15};
int b[2]={20,25};
int c[2] = {30,35};
int *pa[3] = { a,b,c};
1) *(pa+2)[0]   2) *(pa+2)+0   3) *pa+2[0]   4) *(pa+2+0)

정답 : 1 번

10.   포인터 배열에 배열의 주소를 저장하였다. 이에 대한 설명으로 옳은 것은?

1) 2차원 배열과 동일한 인덱스로 참조 가능하다.

2) 배열 간접참조 연산자를 이용하여 2차원 배열 인덱스로 참조해야 한다.

3) 포인터 배열에서는 참조만 가능하고 변경은 불가능하다.

4) 배열명을 포인터 배열에 대입할수 있다.

정답 : 1 번

11.   정수형 배열의 열크기가 4인 2차원 배열의 주소를 보관하기 위한 포인터 변수선언 pr 으로 옳은 것은?

1) int[4] *pr
2) int *pr[4]
3) int (*pr)[4]
4) int *[4] pr

정답 : 3 번

12.   정적지역변수에 대한 설명으로 옳지 않은 것은 무엇인가?

1) 프로그램 전체에서 통용되는 변수이다.
2) 0으로 초기화 된다.
3) 다른 파일에서는 사용할 수 없다.
4) 프로그램 종료 시 해제된다.

정답 : 1 번

13.   전역변수에 대한 설명으로 옳지 않은 것은 무엇인가?

1) 프로그램 전체에서 통용되는 변수이다.
2) 0으로 초기화 된다.
3) char **a다른 파일에서는 사용할 수 없다.
4) 프로그램 종료 시 해제된다.

정답 : 3 번

14.   레지스터 변수에 대한 설명으로 옳지 않은 것은?

1) 레지스터에 할당되는 변수이다.
2) 빠른 액세스가 가능하다.
3) 키워드는 register이다.
4) 전역변수로 사용할 수도 있다.

정답 : 4 번

15.   크기가 12바이트인 구조체를 함수 호출 시 매개변수로 전달하면서 구조체 변수의 주소를 전달했다. 함수에서 구조체를 참조하기 위하여 할당되는 메모리 크기는 얼마인가?

1) 2     2) 4   3) 8   4) 12

정답 : 2 번

16.   함수 sub()에 char 형 배열의 주소가 매개변수로 전달될 때 함수의 형식매개변수로 옳은 것은?
1) char a;
2) char a[]
3) char **a
4) char *a[]
정답 : 2 번

17.   unsigned int 형을 UINT 형으로 이름을 재정의 하는 문장으로 옳은 것은?

1) typedef unsigned int UINT;
2) typedef UINT unsigned int;
3) #typedef unsigned int UINT;
4) #define UINT unsigned int;

정답 : 1 번

18.   다음 코드를 보고 출력결과를 예상하시오.
union st {
   char a;
   int b;
}bmt;

…….
bmt.a=‘a’;
printf(“%d”,bmt.b);

1) 0   2) 63   3) 95   4) 97
정답 : 4 번

19.   다음 공용체의 크기는 얼마인가?

union pay {
  char sel;
  int time;
  double rate;
};

1) 1     2) 4   3) 8   4) 13

정답 : 3 번


20.   다음 구조체에서 포인터 ps를 이용하여 국가 코드를 출력하고자 한다. 옳은 문장은?

struct national {
   char name[20];
   int code;
}korea;
struct *ps =&korea;
…
1) &korea.code
2) ps->code
3) national.code
4) ps.code

정답 : 2 번

21.   다음 코드에서 구조체 선언으로 옳은 문장은?

struct pen {
   char company[20];
   int price;
};

1) pen a;
2) pen price;
3) struct pen a;
4) struct a pen;

정답 : 3 번


22.   다음 구조체 정의에서 구조체 멤버가 아닌 것은?

struct ro{
  int one;
  char ch;
  char name;
} nat;

1) one
2) ro
3) ch
4) name

정답 : 2 번

23.   다음 코드의 [    ]에 옳은 것은?
1) *(p+2)   2) *p[2]      3) *((*p)+2)   4) *p+2

정답 : 3 번
24.   다음 코드의 [    ]에 옳은 것은?

1) (*p)[1]
2) *p[1]
3) *(p+1)
4) p+1

정답 : 1 번

25.   배열 포인터 int (*p)[3] 과 관련한 설명으로 옳은 것은?

1) 주소값3개를 저장할 수 있는 배열이다.
2) 크기가 3인 정수형 배열에 대한 포인터이다.
3) 포인터 변수를 저장하는 배열이다.
4) 정수를 보관하는 포인터 배열이다.

정답 : 2 번

사용
26.   *++p의 값은?

1) 2
2) 4
3) 5
4) 8

정답 : 4 번

27.   다음 중 값이 다른 하나는 무엇인가?
1) arr+1
2) &arr[1]
3) p++
4) p[1]

정답 : 4 번

28.   p++;  *++p의 값은?
1) 15
2) 25
3) 35
4) 36

정답 : 3 번

29.   char 형 포인터 변수의 크기는 일반적인 시스템에서 몇 바이트인가?

1) 1   2) 2   3) 4   4) 8

정답 : 3 번
30.   다음 코드와 관련한 설명으로 옳지 않은 것은?
(1) int st=4;
(2) int *pt = &st
(3) *pt = 9;

1) st의 주소를 포인터 변수 pt에 저장한다.
2) 포인터 변수에 9를 저장한다.
3) st는 데이터 변수이다.
4) 문장 (3)실행 후 st 는 9를 의미한다.

정답 : 2 번

31.   char *p; 와 같이 선언하면 일반적이 시스템에서 크기는 얼마인가?

1) 1
2) 2
3) 4
4) 8

정답 : 3 번


32.   문자 배열에 “I am a boy”를 저장 하려고 한다. 옳은 배열 선언은?

1) char str[11];
2) char str[10];
3) string str
4) string str[10]

정답 : 1 번

33.   함수 gets()에 관련한 설명으로 옳지 않은 것은?

1) 표준 입력으로 입력되는 문자열을 enter키 전까지 저장하고 enter키는 제거한다.
2) 공백을 포함한 문자열 입력이 가능하다.
3) 키보드로 입력되는 문자열을 enter 기 입력될 때까지 입력 받아서 저장한다.
4) 하나의 문자를 입력하는 것도 가능하다.

정답 : 1 번

34.   표준 입력을 통하여 문자열을 입력 시 공백을 포함하여 입력을 저장할 수 있는 함수는?

1) gets()
2) getstring()
3) getch()
4) scanf()

정답 : 1 번

35.   배열 a[2][2]의 초기화 값으로 옳지 않은 것은?

1) {1,2,3,4}
2) {1,2,3,4,5}
3) { {1,2}, {3,4} }
4) {0}

정답 : 2 번

36.   배열에 대한 설명으로 옳지 않은 것은?

1) 동일한 테이터 타입의 묶음이다
2) 동일한 이름으로 참조되는 자료구조이다.
3) 각 요소는 index를 사용하여 참조한다.
4) 최대 3차원 배열까지 사용할 수 있다.

정답 : 4 번

37.   배열 선언 시 배열크기 값으로 사용할 수 없는 것은 무엇인가?

1) 양의 정수
2) 매크로 상수
3) 매크로 상수와의 합 연산식
4) const 상수

정답 : 4 번

38.   전역변수에 관련한 설명으로 잘못된 것은?

1) 전역변수는 함수가 시작하면서 생성되고 함수가 종료되면 소멸된다
2) 전역변수는 자동으로 0으로 초기화된다.
3) 전역변수는 프로그램 전체에서 사용 가능하다.
4) 전역변수는 다른 파일에서 사용하는 것이 가능하다.

정답 : 1 번

39.   전역변수에 대한 설명으로 옳지 않은 것은?
1) 전역변수는 다른 파일에서도 사용 가능하다.
2) 전역변수는 데이터 영역에 생성된다.
3) 전역변수는 묘듈화를 위해 효과적인 프로그래밍 기법이다.
4) 프로그램 종료 시 소멸된다.

정답 : 3 번

40.   다음 프로그램 수행 결과로 옳은 것은?

#include<stdio.h>

void test();

int main()
{
   int m = 22, n = 44;
   printf”%d %d", m, n);
   test();
}
void test()
{
   intm = 50, n = 80;
   printf(“ %d %d", m, n);
}

1) 22 44 22 44
2) 22 44 50 80
3) 50 80 50 80
4) 22 22 44 44

정답 : 2 번

41.   사용자 정의 함수와 관련한 설명으로 잘못된 것은?
1) 매개변수 리스트의 개수나 데이터 형이 다르면 동일한 이름의 함수명을 사용할 수 있다
2) 함수의 리턴형은 함수 내에서 return 문으로 리턴하는 값의 데이터 타입이다.
3) 함수의 이름은 대문자로 사용할 수 있다.
4) void는 리턴이 없는 함수이다.

정답 : 1 번

42.   함수와 관련한 설명으로 옳은 것은?
1) 함수에서 return 문은 여러 개 사용할 수 있다.
2) return 문으로 리턴할 수 있는 값은 최소 한 개 이상이다.
3) return 문에는 변수만 사용할 수 있다.
4) return 수식; 은 불가능하다.
정답 : 1 번

43.   다음 함수 정의와 관련한 설명으로 옳지 않은 것은?
int hot(char a, int b){
}
1) 함수에서 리턴하는 값은 int 형이다.
2) 함수를 호출하면서 함수로 전달되는 값은 2개이다.
3) hot 함수는 2개의 매개변수를 가진 함수이다.
4) 함수 내에 return문은 생략 가능하다.

정답 : 4 번

44.   while 반복문에서 continue 문이 수행된 경우 다음 수행되는 작업에 대한 설명으로 옳은 것은?
1) while의 조건식 을 수행한다.   2) 반복문을 빠져나간다.
3) 증감식을 수행한다.      4) while 이전 문장을 수행한다.
정답 : 1 번
45.   다음 코드에서 break; 문 다음으로 수행되는 문장은 무엇인가?
while(  )
{        while( 문장 1)

        {      문장 2;
               break;             

         }
     문장 3;
}

문장4;
    return 0;
}

1) 문장 1
2) 문장 2
3) 문장 3
4) 문장 4

정답 : 3 번

46.   다음 중 분기문이 아닌 것은 무엇인가?

int a=3;
if (a=2)
  printf(“T”);
else 
  printf(“F”);

1) break
2) goto
3) continue
4) loop

정답 : 4 번

47.   switch (   ) 구문에서 분기를 위한 값으로 사용할 수 없는 것은 무엇인가?

1) 상수
2) 정수
3) 실수
4) 수식

정답 : 3 번




48.   scanf()를 이용하여 문자를 입력 받을 때 이전 입력의 enter로 인해 정상적인 입력이 되지 않는다. 이때 사용할 수 있는 문장은 무엇인가?

1) fflush(stdout);
2) fflush(buffer)
3) fflush(stdin)
4) fflush(stdio)

정답 : 3 번


49.   다음 코드가 수행된 결과는 출력되는 것은 무엇인가?

int a=3;
if (a=2)
  printf(“T”);
else 
  printf(“F”);

1) T
2) F
3) T F
4) 출력 없음

정답 : 1 번

50.   다음 문장 수행 후 변수 a의 값은 무엇인가?

int a=6;    double b=2.0   
a /= b;
1) 6
2) 3
3) 3.0
4) 2

정답 : 2 번

51.   다음 문장 수행 결과 출력되는 값은 무엇인가?

int a=7, b=3;
printf(“%d   %d”, a++, ++a+ ++b);

1) 8 12
2) 9 13
3) 7 12
4) 8 13

정답 : 1 번

52.   임의의 x 값을 3으로 % 연산한 경우 출력 값이 아닌 것은?

1) 0
2) 1
3) 2
4) 3

정답 : 4 번

53.   다음 중 데이터 타입과의 짝이 잘못된 것은 무엇인가?

1) char 문자형
2) int 정수형
3) short 문자형
4) long 정수형

정답 : 3 번

54.   다음 중 식별자로 사용할 수 없는 문자는?

1) 영문대문자
2) 숫자
3) _
4) *

정답 : 4 번

55.   변수에 대한 설명으로 옳지 않은 것은?

1) 프로그램에서 변수란 저장공간의 역할을 한다.
2) 선언과 동시에 초기화 하는 것이 가능하다.
3) 사용하려면 어디서던 선언문만 기술하면 된다.
4) 크기에 따라 데이터 타입을 다르게 선언한다.

정답 : 3 번

56.   다음 중 한줄 주석문에 사용되는 기호는 무엇인가?
1) //
2) /*
3) **
4) /!
정답 : 1 번

57.   다음 중 이스케이프문자와 설명의 짝이 옳지 않은 것은?
1) \n: 커서를 다음 줄로 이동   2) \t: 커서를 탭만큼 이동
3) \b: beep음을 출력함      4) \\: \를 화면에 출력함

정답 : 3 번

58.   C언어에 대한 설명으로 옳지 않은 것은?

1) C언어는 하나 이상의 함수로 이루어진다.
2) 실행 가능한 하나의 프로그램에는 반드시 하나의 main() 함수가 존재한다.
3) 하나의 소스 파일에 하나의 main()이 반드시 존재한다.
4) C언어는 main()함수를 수행하는 언어이다.

정답 : 4 번


59.   IDE에 관한 설명으로 적절하지 않은 것은?

1) 통합개발환경이다.
2) 프로그램개발과 디버깅 실행이 가능하다.
3) IDE가 없으면 프로그래밍이 불가능하다.
4) 컴파일 후 링크도 자동으로 수행가능하다.

정답 : 3 번


60.   소스코드를 기계어코드로 번역하는 것을 무엇이라고 하는가?

1) 코딩
2) 컴파일
3) 링크
4) 디버깅

정답 : 2 번